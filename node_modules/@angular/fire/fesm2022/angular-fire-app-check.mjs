var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from2, except, desc) => {
  if (from2 && typeof from2 === "object" || typeof from2 === "function") {
    for (let key of __getOwnPropNames(from2))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from2[key], enumerable: !(desc = __getOwnPropDesc(from2, key)) || desc.enumerable });
  }
  return to;
};
var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));

// angular-fire-app-check.mjs
var angular_fire_app_check_exports = {};
__export(angular_fire_app_check_exports, {
  AppCheck: () => AppCheck,
  AppCheckInstances: () => ɵAppCheckInstances2,
  AppCheckModule: () => AppCheckModule,
  appCheckInstance$: () => appCheckInstance$,
  getLimitedUseToken: () => getLimitedUseToken,
  getToken: () => getToken,
  initializeAppCheck: () => initializeAppCheck,
  onTokenChanged: () => onTokenChanged,
  provideAppCheck: () => provideAppCheck,
  setTokenAutoRefreshEnabled: () => setTokenAutoRefreshEnabled
});

// public_api.mjs
var public_api_exports = {};
__export(public_api_exports, {
  AppCheck: () => AppCheck,
  AppCheckInstances: () => ɵAppCheckInstances2,
  AppCheckModule: () => AppCheckModule,
  appCheckInstance$: () => appCheckInstance$,
  getLimitedUseToken: () => getLimitedUseToken,
  getToken: () => getToken,
  initializeAppCheck: () => initializeAppCheck,
  onTokenChanged: () => onTokenChanged,
  provideAppCheck: () => provideAppCheck,
  setTokenAutoRefreshEnabled: () => setTokenAutoRefreshEnabled
});

// app-check.mjs
import { ɵAPP_CHECK_PROVIDER_NAME, ɵgetAllInstancesOf } from "@angular/fire";
import { from, timer } from "rxjs";
import { concatMap, distinct } from "rxjs/operators";
var AppCheck = class {
  constructor(appCheck) {
    return appCheck;
  }
};
var appCheckInstance$ = timer(0, 300).pipe(concatMap(() => from(ɵgetAllInstancesOf(ɵAPP_CHECK_PROVIDER_NAME))), distinct());

// app-check.module.mjs
import { isPlatformServer } from "@angular/common";
import { InjectionToken, Injector, NgModule, NgZone, Optional, PLATFORM_ID, isDevMode, makeEnvironmentProviders } from "@angular/core";
import { VERSION, ɵAPP_CHECK_PROVIDER_NAME as ɵAPP_CHECK_PROVIDER_NAME2, ɵAngularFireSchedulers, ɵAppCheckInstances, ɵgetDefaultInstanceOf } from "@angular/fire";
import { FirebaseApp, FirebaseApps } from "@angular/fire/app";
import { registerVersion } from "firebase/app";
import * as i0 from "@angular/core";
var PROVIDED_APP_CHECK_INSTANCES = new InjectionToken("angularfire2.app-check-instances");
function defaultAppCheckInstanceFactory(provided, defaultApp) {
  const defaultAppCheck = ɵgetDefaultInstanceOf(ɵAPP_CHECK_PROVIDER_NAME2, provided, defaultApp);
  return defaultAppCheck && new AppCheck(defaultAppCheck);
}
var LOCALHOSTS = ["localhost", "0.0.0.0", "127.0.0.1"];
var isLocalhost = typeof window !== "undefined" && LOCALHOSTS.includes(window.location.hostname);
function appCheckInstanceFactory(fn) {
  return (zone, injector, platformId) => {
    if (!isPlatformServer(platformId) && (isDevMode() || isLocalhost)) {
      globalThis.FIREBASE_APPCHECK_DEBUG_TOKEN ??= true;
    }
    const appCheck = zone.runOutsideAngular(() => fn(injector));
    return new AppCheck(appCheck);
  };
}
var APP_CHECK_INSTANCES_PROVIDER = {
  provide: ɵAppCheckInstances,
  deps: [
    [new Optional(), PROVIDED_APP_CHECK_INSTANCES]
  ]
};
var DEFAULT_APP_CHECK_INSTANCE_PROVIDER = {
  provide: AppCheck,
  useFactory: defaultAppCheckInstanceFactory,
  deps: [
    [new Optional(), PROVIDED_APP_CHECK_INSTANCES],
    FirebaseApp,
    PLATFORM_ID
  ]
};
var AppCheckModule = class _AppCheckModule {
  constructor() {
    registerVersion("angularfire", VERSION.full, "app-check");
  }
  static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: _AppCheckModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule });
  static ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "18.0.0-rc.3", ngImport: i0, type: _AppCheckModule });
  static ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: _AppCheckModule, providers: [
    DEFAULT_APP_CHECK_INSTANCE_PROVIDER,
    APP_CHECK_INSTANCES_PROVIDER
  ] });
};
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: AppCheckModule, decorators: [{
  type: NgModule,
  args: [{
    providers: [
      DEFAULT_APP_CHECK_INSTANCE_PROVIDER,
      APP_CHECK_INSTANCES_PROVIDER
    ]
  }]
}], ctorParameters: () => [] });
function provideAppCheck(fn, ...deps) {
  registerVersion("angularfire", VERSION.full, "app-check");
  return makeEnvironmentProviders([
    DEFAULT_APP_CHECK_INSTANCE_PROVIDER,
    APP_CHECK_INSTANCES_PROVIDER,
    {
      provide: PROVIDED_APP_CHECK_INSTANCES,
      useFactory: appCheckInstanceFactory(fn),
      multi: true,
      deps: [
        NgZone,
        Injector,
        PLATFORM_ID,
        ɵAngularFireSchedulers,
        FirebaseApps,
        ...deps
      ]
    }
  ]);
}

// firebase.mjs
var firebase_exports = {};
__export(firebase_exports, {
  getLimitedUseToken: () => getLimitedUseToken,
  getToken: () => getToken,
  initializeAppCheck: () => initializeAppCheck,
  onTokenChanged: () => onTokenChanged,
  setTokenAutoRefreshEnabled: () => setTokenAutoRefreshEnabled
});
__reExport(firebase_exports, app_check_star);
import * as app_check_star from "firebase/app-check";
import { ɵzoneWrap } from "@angular/fire";
import { getLimitedUseToken as _getLimitedUseToken, getToken as _getToken, initializeAppCheck as _initializeAppCheck, onTokenChanged as _onTokenChanged, setTokenAutoRefreshEnabled as _setTokenAutoRefreshEnabled } from "firebase/app-check";
var getLimitedUseToken = ɵzoneWrap(_getLimitedUseToken, true);
var getToken = ɵzoneWrap(_getToken, true);
var initializeAppCheck = ɵzoneWrap(_initializeAppCheck, true);
var onTokenChanged = ɵzoneWrap(_onTokenChanged, true);
var setTokenAutoRefreshEnabled = ɵzoneWrap(_setTokenAutoRefreshEnabled, true);

// public_api.mjs
__reExport(public_api_exports, firebase_exports);
import { ɵAppCheckInstances as ɵAppCheckInstances2 } from "@angular/fire";

// angular-fire-app-check.mjs
__reExport(angular_fire_app_check_exports, public_api_exports);
export {
  AppCheck,
  ɵAppCheckInstances2 as AppCheckInstances,
  AppCheckModule,
  appCheckInstance$,
  getLimitedUseToken,
  getToken,
  initializeAppCheck,
  onTokenChanged,
  provideAppCheck,
  setTokenAutoRefreshEnabled
};
//# sourceMappingURL=angular-fire-app-check.mjs.map
