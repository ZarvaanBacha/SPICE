// auth-guard.mjs
import { Injectable } from "@angular/core";
import { AngularFireAuth } from "@angular/fire/compat/auth";
import { Router } from "@angular/router";
import { of, pipe } from "rxjs";
import { map, switchMap, take } from "rxjs/operators";
import * as i0 from "@angular/core";
import * as i1 from "@angular/router";
import * as i2 from "@angular/fire/compat/auth";
var loggedIn = map((user) => !!user);
var AngularFireAuthGuard = class _AngularFireAuthGuard {
  router;
  auth;
  constructor(router, auth) {
    this.router = router;
    this.auth = auth;
  }
  canActivate = (next, state) => {
    const authPipeFactory = next.data.authGuardPipe || (() => loggedIn);
    return this.auth.user.pipe(take(1), authPipeFactory(next, state), map((can) => {
      if (typeof can === "boolean") {
        return can;
      } else if (Array.isArray(can)) {
        return this.router.createUrlTree(can);
      } else {
        return this.router.parseUrl(can);
      }
    }));
  };
  static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: _AngularFireAuthGuard, deps: [{ token: i1.Router }, { token: i2.AngularFireAuth }], target: i0.ɵɵFactoryTarget.Injectable });
  static ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: _AngularFireAuthGuard, providedIn: "any" });
};
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i0, type: AngularFireAuthGuard, decorators: [{
  type: Injectable,
  args: [{
    providedIn: "any"
  }]
}], ctorParameters: () => [{ type: i1.Router }, { type: i2.AngularFireAuth }] });
var canActivate = (pipe2) => ({
  canActivate: [AngularFireAuthGuard],
  data: { authGuardPipe: pipe2 }
});
var isNotAnonymous = map((user) => !!user && !user.isAnonymous);
var idTokenResult = switchMap((user) => user ? user.getIdTokenResult() : of(null));
var emailVerified = map((user) => !!user && user.emailVerified);
var customClaims = pipe(idTokenResult, map((idTokenResult2) => idTokenResult2 ? idTokenResult2.claims : []));
var hasCustomClaim = (
  // eslint-disable-next-line no-prototype-builtins
  (claim) => pipe(customClaims, map((claims) => claims.hasOwnProperty(claim)))
);
var redirectUnauthorizedTo = (redirect) => pipe(loggedIn, map((loggedIn2) => loggedIn2 || redirect));
var redirectLoggedInTo = (redirect) => pipe(loggedIn, map((loggedIn2) => loggedIn2 && redirect || true));

// auth-guard.module.mjs
import { NgModule } from "@angular/core";
import { VERSION } from "@angular/fire";
import firebase from "firebase/compat/app";
import * as i02 from "@angular/core";
var AngularFireAuthGuardModule = class _AngularFireAuthGuardModule {
  constructor() {
    firebase.registerVersion("angularfire", VERSION.full, "auth-guard-compat");
  }
  static ɵfac = i02.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i02, type: _AngularFireAuthGuardModule, deps: [], target: i02.ɵɵFactoryTarget.NgModule });
  static ɵmod = i02.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "18.0.0-rc.3", ngImport: i02, type: _AngularFireAuthGuardModule });
  static ɵinj = i02.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i02, type: _AngularFireAuthGuardModule, providers: [AngularFireAuthGuard] });
};
i02.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "18.0.0-rc.3", ngImport: i02, type: AngularFireAuthGuardModule, decorators: [{
  type: NgModule,
  args: [{
    providers: [AngularFireAuthGuard]
  }]
}], ctorParameters: () => [] });
export {
  AngularFireAuthGuard,
  AngularFireAuthGuardModule,
  canActivate,
  customClaims,
  emailVerified,
  hasCustomClaim,
  idTokenResult,
  isNotAnonymous,
  loggedIn,
  redirectLoggedInTo,
  redirectUnauthorizedTo
};
//# sourceMappingURL=angular-fire-compat-auth-guard.mjs.map
