export interface Spice {
    id: number;
    name: string;
    quantity: number;
} //TODO : move this to a common model file and possibly just change its implementation to fit the SpiceContainer interface
